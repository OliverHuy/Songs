<%= form_for @singer, html: { multipart: true } do |f| %><br>
<p>
	<%= f.label :image %><br>
	<%= f.file_field :image %><br>
	<%= f.label :name %><br>
	<%= f.text_field :title %><br>
	<%= f.label :Information %><br>
	<%= f.text_area :body %><br>
	<%= f.submit %><br>
	<% end %> 
</p>